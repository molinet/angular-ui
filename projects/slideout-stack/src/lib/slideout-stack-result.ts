/**
 * Result returned by the slideout when popped.
 * @author molinet
 */
export interface SlideOutStackResult {
  /**
   * Data returned by the slideout when popped.
   */
  data?: any;
  /**
   * Role returned by the slideout when popped.
   */
  role?: string;
}
